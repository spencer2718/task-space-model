# Environment
.env
.venv/
venv/
env/

# Cache (general)
.cache/*
!.cache/artifacts/
!.cache/artifacts/v1/
!.cache/artifacts/v1/**

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data
data/raw/*
!data/raw/.gitkeep
# O*NET and processed data: tracked for reproducibility
# data/onet/ — tracked
# data/processed/ — tracked

# Outputs
outputs/*
!outputs/.gitkeep
!outputs/experiments/
!outputs/experiments/**

# Models (too large for git)
models/*
!models/.gitkeep

# Killshot data (large files excluded, results kept)
killshot/cps_mobility/data/*.dat.gz
killshot/cps_mobility/data/*.parquet
killshot/cps_mobility/data/*.npz
killshot/mobility_feasibility/data/crosswalks/*.xls
killshot/mobility_feasibility/data/crosswalks/*.xlsx
killshot/mobility_feasibility/outputs/*.npz

# Node
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# External data — tracked for reproducibility, except raw IPUMS census extracts
# (IPUMS extracts can be re-downloaded from https://ipums.org if needed)
data/external/ipums/census/*.dat.gz
